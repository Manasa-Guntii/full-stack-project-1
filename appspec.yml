version: 0.0
os: linux

files:
  - source: demo/target/demo-0.0.1-SNAPSHOT.jar
    destination: /home/ec2-user/app

hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      runas: ec2-user
      timeout: 300